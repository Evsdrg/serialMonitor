[tool:pytest]
addopts = -ra -q --strict-markers
testpaths = tests
markers =
    slow: marks tests as slow (integration/property tests, deselect with '-m "not slow"')
    integration: marks tests that use real PTY or external systems
    hardware: marks tests that require actual hardware
    qt: marks tests that require a Qt application
